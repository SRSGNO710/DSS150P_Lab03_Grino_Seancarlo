# Pipeline dependencies (local venv and the `pipeline` Docker image).
# Pinned for reproducibility; wheels exist for Python 3.10-3.13.
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
psycopg2-binary==2.9.10
PyYAML==6.0.2
python-dotenv==1.0.1
pytest==8.3.4
