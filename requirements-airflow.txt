# Extra packages installed into apache/airflow:2.10.5-python3.11 by Dockerfile.airflow,
# so the Airflow worker can run `python -m src.cli ...` directly.
# Pinned to versions compatible with Airflow 2.10.5 on Python 3.11.
pandas==2.1.4
pyarrow==16.1.0
psycopg2-binary==2.9.10
PyYAML==6.0.2
python-dotenv==1.0.1
